# Tree-sitter Micron

Grammar for NomadNet Micron (`.mu`), aligned with micron-parser-go and NomadNet Python.

Generate the parser with the tree-sitter CLI when available:

```text
cd tree-sitter-micron && npx tree-sitter generate
```

File types: `mu`. Scope: `source.micron`.
